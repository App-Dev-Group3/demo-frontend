
  # Form Approval Workflow System

  This is a code bundle for Form Approval Workflow System. The original project is available at https://www.figma.com/design/HIHFG0I6R1Za3Z1R4OPTFk/Form-Approval-Workflow-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  